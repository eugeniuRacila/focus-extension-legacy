/**
 * Set-up the styles for the body element of the user's active tab.
 * @param body active tab document's body element
 */
export const setupBodyStyles = (body: HTMLBodyElement) => {
  body.style.overflow = 'hidden';
  body.style.position = 'relative';
};
